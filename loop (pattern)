#include<stdio.h>
#include<conio.h>
int main()
{
int i,j,rows;
printf("Enter no.of rows:");
scanf("&d",&rows);
for(i=rows;i>=1;--i)
{
  for(j=1;j<=i;++j)
{
  printf("01",j);
}
  printf("\n");
}
  return 0;
}
